# Disk-Scheduling-Simulator
Python GUI app simulates disk scheduling (FCFS, SSTF, SCAN, C-SCAN, LOOK). Users input requests, head position, cylinders. Animates head movement, shows metrics (seek time, throughput), plots comparisons, finds best algorithm. Features random requests, adjustable speed, reset. Built with Tkinter, Matplotlib.
